[package]
name = "rust_driver"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
test = false
bench = false

[dependencies]
wdk = "0.2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"